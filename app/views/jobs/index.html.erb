<h1>Exibindo <%= pluralize(@jobs.count, 'anúncio') %></h1>

<table>

<% @jobs.each do |job| %>
  <tr>
    <td><%= link_to job.title, job %></td>
    <td colspan="2"><%= job.location %></td>
  </tr>
  <tr>
    <td>Empresa contratante</td>
    <td colspan="2"><%= timeago(job.created_at) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job', new_job_path %>
