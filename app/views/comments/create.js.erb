/* Insert a notice between the last comment and the comment form */
$("#comment-notice").html('<div class="flash-notice round"><%= escape_javascript(flash.delete(:notice)) %></div>');

/* Replace the count of comments */
$("#comments_count").html("<%= pluralize(@total, 'Comentário') %>");

/* Add the new comment to the bottom of the comments list */
$("#comments").append("<%= escape_javascript(render(@comment)) %>");

/* Hide the notice with slideup effect slower */
$("#comment-notice").slideUp(5000);

/* Highlight the new comment */
$("div#comment_<%= @comment.id %>").effect("highlight", {}, 3000); /* this is not working */

/* Reset the comment form */
$("#comments_form")[0].reset(); /* this is not working */
